# Frontend

Este diretório contém uma página estática (`index.html`) que consome a API do backend para:
- Listar benefícios
- Criar novo benefício
- Realizar transferência entre benefícios

Como usar:
- Inicie o backend em `http://localhost:8080`
- Abra `index.html` no navegador (duplo clique ou sirva estaticamente)